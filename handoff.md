# AI A → AI B 인수인계 문서

## 1. 목표

`card-studio`의 템플릿 저장 및 편집 기능에서 템플릿 이름 중복 저장을 방지한다.

고정 정책:

* 템플릿 이름 비교 시 앞뒤 공백을 제거한다.
* 빈 문자열 또는 공백만 있는 이름은 저장할 수 없다.
* 동일한 템플릿 이름은 중복 저장할 수 없다.
* 편집 시에도 다른 템플릿과 동일한 이름으로 변경할 수 없다.
* 영문 대소문자는 구분한다. (`Travel`과 `travel`은 서로 다른 이름)

## 2. 현재 상태

인수인계 기준 저장소 버전:
`e31be38`

AI A는 `script.js`에 템플릿 이름 중복 검사 로직을 추가했다.

새 템플릿 저장 시:

* 입력 이름을 `trim()`하여 앞뒤 공백을 제거한다.
* 빈 이름을 거부한다.
* 기존 템플릿과 동일한 이름인지 검사한다.
* 중복이면 저장하지 않고 중복 안내를 표시한다.

템플릿 편집 시:

* 입력 이름을 `trim()`한다.
* 빈 이름을 거부한다.
* 다른 템플릿과 이름이 중복되는 경우 변경하지 않는다.
* 기존 이름을 그대로 유지하는 편집은 허용한다.

AI A가 추가한 파일:
`test_templates.js`

AI A의 작업은 완전히 종료된 상태가 아니며, 학생이 AI A의 부분 작업을 `e31be38` 커밋으로 보존했다.

## 3. 실행 명령

프로젝트 폴더에서 다음 명령으로 웹 애플리케이션을 실행한다.

```bash
cd ~/Ritual/card-studio
python3 -m http.server 8080
```

브라우저에서 다음 주소를 연다.

```text
http://127.0.0.1:8080/
```

자동 테스트 파일은 다음 명령으로 실행할 수 있다.

```bash
cd ~/Ritual/card-studio
node test_templates.js
```

단, 현재 `test_templates.js` 실행에는 오류가 있다.

## 4. 통과 검사

작업 시작 전에 고정한 검사는 T01~T10이며, `assignment5-baseline.txt`에 기록되어 있다.

* T01: 이름 `여행` 저장 → 템플릿 1개
* T02: `여행` 중복 저장 → 저장되지 않고 중복 안내
* T03: `맛집` 저장 → 저장됨
* T04: `맛집` 저장 → 중복으로 처리
* T05: 공백만 입력 → 저장되지 않고 이름 오류 안내
* T06: `운동` 저장 → 총 3개
* T07: `여행`을 `맛집`으로 편집 → 변경되지 않음
* T08: `여행`을 그대로 유지하며 편집 → 정상 저장
* T09: `여행`을 `여행`으로 변경 → 중복으로 처리
* T10: `Travel`이 있을 때 `travel` 저장 → 새 템플릿으로 저장

현재 AI A가 만든 자동 테스트는 T01~T10 전체 결과를 확정하지 못했다.

## 5. 남은 문제

AI A가 만든 `test_templates.js`를 실행하면 다음 오류가 발생한다.

```text
SyntaxError: Identifier 'imageInput' has already been declared
```

오류는 `test_templates.js`의 `resetState()`에서 `script.js`를 VM으로 다시 실행하는 과정에서 발생한다.

따라서 현재 자동 테스트 실행 결과를 전체 PASS로 판단해서는 안 된다.

또한 AI A의 실제 CLI 사용량은 총 26회로, 작업 시작 전에 정한 공통 상한인 10회를 초과했다.

AI A는 사용량 제한에 도달하여 정식 인수인계 문서를 작성하지 못했고 커밋도 하지 못했다. 학생이 당시 작업 상태를 보존하기 위해 `e31be38` 커밋을 만들었다.

## 6. 다음 행동

AI B는 AI A와 동일한 고정 검사 T01~T10과 동일한 요구사항을 기준으로 작업한다.

우선 현재 저장소와 이 인수인계 문서를 확인한다.

그 다음:

1. 현재 `script.js`의 템플릿 저장·편집 로직을 확인한다.
2. 템플릿 이름 중복 방지 기능이 T01~T10의 기대값을 만족하는지 검증한다.
3. 필요한 수정이 있다면 최소 범위에서 수정한다.
4. 동일한 T01~T10을 다시 실행하여 결과를 기록한다.
5. 기존 고정 검사 자체는 삭제하거나 완화하거나 기대값을 변경하지 않는다.
6. 작업 완료 후 변경 파일, 검사 결과, 실행 방법, 최종 저장소 버전을 기록한다.

AI B의 작업 시간은 공통 상한 30분 이내로 유지한다.

AI B의 요청·호출 수 역시 공통 상한 10회 이내로 관리한다.

## 7. 건드리지 말 것

다음 항목은 AI B가 임의로 변경하지 않는다.

* `assignment5-baseline.txt`의 고정 검사 T01~T10
* 고정 검사 ID
* 고정 검사 입력
* 고정 검사 기대값
* 공통 시간 상한 30분
* 공통 요청·호출 상한 10회
* `card-studio`의 기존 이미지 편집 기능
* 기존 템플릿 생성·삭제·불러오기 기능
* JSON 가져오기·내보내기 기능
* 다운로드 기능
* 외부 라이브러리 또는 서버 추가
* 이번 개선과 관계없는 UI 및 기능

이번 작업의 범위는 템플릿 이름 중복 저장 방지 기능과 이를 검증하기 위한 작업에 한정한다.
